name: CI

on:
  pull_request:
  push:

jobs:
  test:
    name: CI Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Test
        run: echo "CI check passed"